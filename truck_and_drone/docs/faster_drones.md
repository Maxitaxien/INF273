For the last assignment - just deterministically recompute optimal drone coverings for each modified solution.
This is a lot easier and robust and is actually not that slow. Then we only need to worry about doing truck route changes,
then we run this `recompute_drones` func.
