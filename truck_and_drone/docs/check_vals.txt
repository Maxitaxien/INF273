for (std::vector<long long> line : problem_instance.drone_matrix) {
    for (long long val : line) {
        std::cout << val << " ";
    }
    std::cout << "\n";
}


for (const auto& [launch, vec] : drone_cover_solution.drone_map) {
    for (const auto& p : vec) {
        int deliver = p.first;
        int land = p.second;
        std::cout << "Launch: " << launch << " deliver: " << deliver << ", land: " << land << "\n";
    }
}
