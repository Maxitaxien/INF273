Run the following commands inside /build:

cmake .. (only on first run of project)
cmake --build .
./truck_and_drone

(Optionally: make -j$(nproc) to use all cpu cores in build)

Remember to include new files as dependencies in CMakeLists.txt!